/*
 * PersonAdd Messages
 *
 * This contains all the text for the PersonAdd container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PersonAdd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PersonAdd container!',
  },
});
